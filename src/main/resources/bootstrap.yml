spring:
  applicaton:
    name: admin
  cloud:
    kubernetes:
      discovery:
        all-namespaces: true
        service-labels:
          spring-boot: true
logging:
  level:
    org.springframework.web: DEBUG
    com.jio.partneranalytics: INFO

server:
  port: 8081